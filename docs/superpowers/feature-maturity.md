# Vanrot Feature Maturity Ledger

**Date:** 2026-05-25
**Purpose:** Track whether each Vanrot capability is only planned, demo-capable, production-ready, complete, or
deferred.

This ledger is the source of truth for feature maturity across phases. Phase checkboxes show delivery milestones. This
file shows capability maturity.

Demo phases 0 through 10 are complete. Phase 11 starts the production-standard track.

Future implementation candidates that are not active, shipped, or explicitly dropped are parked in
`docs/superpowers/future-pipeline.md` until their individual brainstorming, spec, and writing-plan passes become active.

Use the production roadmap checklist below like the phase checklist in `docs/brainstorm.md`: choose the next unchecked
production phase, run brainstorming, write a spec, write a plan, implement, verify, then update this ledger.

Rows or sections marked with `*` are brainstorming-level pillars. They must get a dedicated brainstorming, design spec,
written plan, and review before implementation starts.

## Status Values

```txt
Planned           known work, not implemented
In Progress       actively being implemented in another branch or workspace
Demo-Capable      works for the milestone demo with intentional limits
Production-Ready  stable, documented, tested for edge cases, and integrated
Complete          non-runtime foundation work is done and verified
Deferred          intentionally outside the current MVP slice
Blocked           depends on a missing upstream decision or implementation
```

## Package And Module Map

Each package section is divided into module or submodule tables. This keeps production planning focused: pick a package,
choose a module, then write a spec and plan for the deferred production gates.

## Production Roadmap Checklist

Status:

```txt
[ ] not started
[x] done
```

| Done |    Phase | Track                                             | Modules And Submodules                                                                                                                                                                                   | Tick When                                                                                                                                                                                                                                          |
|------|---------:|---------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| [x]  | Phase 11 | Production roadmap and standards foundation       | Feature maturity ledger, production gates, phase order, package/module ownership, release-readiness vocabulary                                                                                           | `docs/superpowers/feature-maturity.md` has a detailed production roadmap with numbered phases, package modules, submodules, and acceptance gates.                                                                                                  |
| [x]  | Phase 12 | Core framework hardening                          | `@vanrot/runtime`, `@vanrot/compiler`, `@vanrot/vite-plugin`, TypeScript typing, diagnostics, source maps, HMR, compiler control flow                                                                    | Runtime/compiler/Vite demo features become production-ready enough for real apps, with edge cases, diagnostics, source maps, and typing gaps addressed.                                                                                            |
| [x]  | Phase 13 | Project configuration system                      | `vanrot.config.ts`, config schema, config validation, install-aware defaults, Vite/CLI config loading, default port `1010`                                                                               | Generated apps use a typed root `vanrot.config.ts`; installed Vanrot packages auto-populate only the config blocks they need.                                                                                                                      |
| [x]  | Phase 14 | CLI product experience slice                      | Grouped root help, aligned reporter labels, stable CLI diagnostic catalog, structured output flags, and `.vanrot/ai` doorway files                                                                       | The implemented CLI surface is clearer, scriptable, and AI-readable while broader `inspect`, cache, and localhost work remains tracked below.                                                                                                      |
| [x]  | Phase 15 | Router production                                 | 15A route contract production, 15B nested layout routing, 15C navigation decisions, 15D preloading/keepAlive integration, and 15E navigation polish are complete                                         | `@vanrot/router` supports normal application routing needs without repeated route string literals in user templates.                                                                                                                               |
| [ ]  | Phase 16 | UI October production                             | 16A foundation, 16B core primitives, 16C Vanrot learning site base, 16D layout/navigation/media, 16E forms/data, 16F interaction foundation, 16G final October polish, and 16H behavior package boundary | `@vanrot/ui` has a shadcn-inspired October component system with copyable, editable, accessible Vanrot-native primitives, final interaction polish, a dogfooded public learning site, and optional behavior helpers split out of the core runtime. |
| [ ]  | Phase 17 | Post-production implementation: brutalist UI      | Brutalist flavor, flavor selection, token separation, component parity with October, visual QA                                                                                                           | Removed from the future pipeline as not needed unless the user explicitly reopens it.                                                                                                                                                              |
| [x]  | Phase 18 | Post-production implementation: testing           | `testPage(...)`, generator-wide `--test`, accessibility assertions, async helpers, router helpers, forms testing, fake timers                                                                            | `@vanrot/testing` now covers page tests, router setup, accessibility assertions, async/fake-timer helpers, and generator-wide `--test` support while deferring forms/resource helpers to Phase 21.                                                 |
| [x]  | Phase 19 | Post-production implementation: store core        | Signal-native store package, state containers, actions, reducers, selectors, full effects, lightweight enterprise patterns                                                                                | `@vanrot/store` provides a future-facing enterprise state foundation without copying NgRx or requiring RxJS by default. Store Foundation shipped from `docs/superpowers/future-pipeline.md` with docs, AI docs, examples, and size verification.       |
| [x]  | Phase 20 | Post-production implementation: store hardening   | Deeper cancellation and concurrency inspection, Vanrot-native action/state/effect observation, snapshots, replay contracts, and headless devtools-ready inspection                                      | `@vanrot/store` now exposes `inspectStore`, bounded event history, snapshots, replay, and effect lifecycle events without adding Redux, RxJS, or UI responsibilities to the store package. Devtools UI repair remains future pipeline work.          |
| [x]  | Phase 21 | Post-production implementation: forms and async   | `@vanrot/forms`, field state, validation, field metadata, async resources, arrays, wizards, server errors, drafts, diagnostics, docs, and testing helpers                                                | Vanrot has first-party form-heavy and async-heavy app patterns without bloating the runtime kernel. Standalone resources, forms-aware compiler two-way binding, and `@await` or `@defer` syntax remain deferred in `docs/superpowers/future-pipeline.md`. |
| [x]  | Phase 22 | Post-production implementation: SSR and hydration | SSR-safe APIs, server rendering, hydration, mismatch diagnostics, routing integration, streaming strategy                                                                                                | Vanrot ships opt-in `@vanrot/ssr` server rendering, hydration attach, mismatch diagnostics, router SSR, compiler server output, and docs. Streaming remains deferred.                                                                              |
| [x]  | Phase 23 | Devtools and project intelligence hardening       | Runtime graph inspection, project map graphing, route graph, component graph, compiler-aware metadata, AI rules customization                                                                            | Developers and AI tools can inspect Vanrot project structure and runtime behavior from authoritative local metadata.                                                                                                                               |
| [x]  | Phase 24 | Documentation and web presence                    | Deep docs, API docs, guide docs, docs completeness checks, `vanrot.vankode.com`, install guide, UI docs                                                                                                  | Public documentation covers every package, command, convention, generated file, limitation, and production caveat.                                                                                                                                 |
| [x]  | Phase 25 | AI consumption                                    | AI-readable docs bundle, framework knowledge manifest, MCP server, Skill.sh package, optional AI commands                                                                                                | Codex, Claude, and other AI tools can consume official Vanrot knowledge instead of guessing framework behavior.                                                                                                                                    |
| [x]  | Phase 26 | Distribution and release hardening                | npm publishing, package provenance, release CI, versioning, changelog, Homebrew formula, install verification                                                                                            | Vanrot now has a strict local release dry-run gate for packed npm artifacts, pnpm/npm consumers, and local Homebrew tap verification before public publishing.                                                                                     |
| [x]  | Phase 27 | First-party SEO package                           | `@vanrot/seo`, SEO config domain, metadata ladder, sitemap and robots outputs, create/add opt-in, doctor diagnostics, docs guide                                                                         | SEO can be installed at project creation or added later without runtime bloat, with typed metadata helpers, generated app utilities, build outputs, and diagnostics.                                                                               |
| [x]  | Phase 28 | Behavior Expansion                                | Accordion/collapsible/disclosure, selection/listbox/select/combobox, menus, toggle/toolbars, scroll areas, portals, focus utilities, calendars, drag/drop, and table resizing                          | `@vanrot/behavior` ships the expanded headless interaction suite with subpath exports, config/CLI helper names, docs, AI bundle coverage, and TDD verification.                                                                                    |
| [x]  | Phase 29 | Formatters and template pipes                     | `@vanrot/formatters`, compiler-owned pipe syntax, built-in formatters, custom `.pipe.ts` files, named presets, config context, Vite diagnostics, docs, examples, and release coverage                  | Vanrot ships Angular-style interpolation pipes as a compiler feature backed by a small formatter package while keeping `@vanrot/runtime` unchanged.                                                                                                |
| [ ]  | Phase 30 | Docs page components                              | Real docs page components, docs shared article components, page-local role files, docs tree metadata, AI/search compatibility, and route/render guards                                                  | Human-authored framework docs live in `.page.ts`, `.page.html`, and `.page.css` triplets without changing the visible docs design.                                                                                                                 |
| [x]  | Phase 31 | Editor tooling release                            | JetBrains plugin release, Web Types, language-server project intelligence, template navigation, route rename, diagnostics, code actions, docs IA, AI docs, and packaging checks                        | IntelliJ IDEA Ultimate and WebStorm users get coherent Vanrot recognition, completion, navigation, diagnostics, fixes, and bundled language-server behavior from shared sources of truth.                                                           |

## Repo

### Workspace Foundation

| Feature                       | Package or Area | Planned Phase | Demo-Capable Gate                           | Production-Ready Gate                                              | Status   | Notes                                                                                      |
|-------------------------------|-----------------|--------------:|---------------------------------------------|--------------------------------------------------------------------|----------|--------------------------------------------------------------------------------------------|
| Monorepo workspace foundation | repo            |       Phase 1 | Core package shells build as empty packages | Workspace scripts, TS config, tests, and clean builds are verified | Complete | `packages/runtime`, `packages/compiler`, `packages/vite-plugin`, and `packages/cli` exist. |

### Production Standards Foundation

| Feature                      | Package or Area | Planned Phase | Demo-Capable Gate                                                  | Production-Ready Gate                                                                                                                             | Status           | Notes                                                                                               |
|------------------------------|-----------------|--------------:|--------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------|------------------|-----------------------------------------------------------------------------------------------------|
| Production phase checklist   | repo and docs   |      Phase 11 | Production work is split into numbered phases after the demo track | Every production phase has package/module ownership, submodules, acceptance gates, and update rules                                               | Production-Ready | Turns Phase 11 into the production roadmap foundation instead of a catch-all implementation bucket. |
| Production readiness gates   | repo and docs   |      Phase 11 | Production-ready status requires more than passing demos           | Each package has documented behavior, edge-case tests, integration tests, diagnostics, and known limitations before being marked production-ready | Production-Ready | Prevents demo-capable features from being accidentally treated as release-ready.                    |
| Package/module ownership map | repo and docs   |      Phase 11 | Features are grouped by package and module                         | Every production phase can be traced to package sections and submodule rows in this ledger                                                        | Production-Ready | Keeps the roadmap useful for planning without losing package-level lookup.                          |

### Production Audit Infrastructure

| Feature                         | Package or Area  |      Planned Phase | Demo-Capable Gate                                                                        | Production-Ready Gate                                                                                                              | Status           | Notes                                                                                                                                                                                                                                        |
|---------------------------------|------------------|-------------------:|------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------|------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Core production audit lane      | repo and tests   |           Phase 12 | `pnpm audit:core` runs isolated red tests for known production gaps                      | Each audit test is either converted into normal passing coverage by its owning slice or remains tracked in the final TDD inventory | Production-Ready | Phase 12A created the red lane. Phase 12B burned down runtime audit failures, Phase 12C burned down compiler audit failures, Phase 12D burned down Vite plugin audit failures, and Phase 12E burned down TypeScript contract audit failures. |
| Final TDD inventory growth rule | repo and docs    |           Phase 12 | `docs/superpowers/final-tdd-inventory.md` is pre-populated and required by durable rules | Phase 26 uses the inventory for complete release TDD across the whole framework                                                    | Demo-Capable     | Every future production phase must update the inventory when framework surface area changes.                                                                                                                                                 |
| Runtime size budget protocol    | runtime and repo | Phase 2, Phase 16H | `packages/runtime/.size-limit.json` caps the browser runtime bundle                      | `pnpm verify:size` and the phase-docs test enforce the 1.98 KB cap and escalation rule before future increases                     | Production-Ready | Optional headless behavior controllers live in `@vanrot/behavior`; if the runtime cap is hit or breached, report the exact size and explain which core runtime feature caused it before raising it.                                          |

## Project Configuration *

*Vanrot should use the standard root config layout by default: `package.json`, `tsconfig.json`, `vite.config.ts`,
and `vanrot.config.ts` live at the app root. `package.json` remains the ecosystem-owned source for scripts and
dependencies, while `vanrot.config.ts` becomes Vanrot's typed source of truth for framework conventions.*

### Root Config And Install-Aware Defaults

| Feature                                | Package or Area          | Planned Phase | Demo-Capable Gate                                                                                                                                                 | Production-Ready Gate                                                                                                         | Status           | Notes                                                                                                                                                      |
|----------------------------------------|--------------------------|--------------:|-------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------|------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Root `vanrot.config.ts` *              | config, cli, vite-plugin |      Phase 13 | A generated app includes a small typed Vanrot config at the root                                                                                                  | Full config schema, defaults, validation, migrations, docs, examples, and package integration verified                        | Production-Ready | Generated apps now include canonical `vanrot.config.ts` with `defineVanrotConfig(...)`, schema version, `source.root`, and `devServer.port`.               |
| Full production config schema *        | config                   |      Phase 13 | Config can describe source roots, routes, UI, generator defaults, diagnostics, dev server, build budgets, testing, cache, docs, and AI/project intelligence hooks | Schema versioning, typed helper API, validation diagnostics, migrations, docs, and backwards compatibility verified           | Production-Ready | `@vanrot/config` now owns typed config domains, normalization defaults, shared diagnostics (`VRCFG###`), loader, migrations, recovery, and editor helpers. |
| Install-aware config auto-population * | cli and config           |      Phase 13 | `vr create`, `vr add`, and future package installers add only the config blocks required by installed Vanrot packages                                             | Idempotent writes, comments/preservation policy, uninstall behavior, conflict detection, migrations, tests, and docs verified | Production-Ready | `vr add button` now upserts the `ui` domain idempotently through shared config editor helpers and preserves existing customized domains.                   |
| Optional behavior config               | config and cli           |     Phase 16H, Phase 28 | `behavior.enabled` can describe selected optional behavior helpers across the moved and expanded behavior suites | Defaults, validation diagnostics, generated create output, remove cleanup, docs, and AI bundle references verified            | Production-Ready | `vr create --behavior` writes this domain only when behavior helpers are selected; `vr remove behavior` prunes it safely.                                  |
| Formatting config domain               | config, compiler, vite-plugin |      Phase 29 | `formatting.locale`, `formatting.timezone`, and `formatting.currency` normalize into compiler pipe context | Defaults, validation diagnostics, generated output, docs, examples, and AI bundle references verified                         | Production-Ready | `@vanrot/config` now supplies locale, timezone, and currency defaults for `@vanrot/formatters` without changing `@vanrot/runtime`.                         |
| Config single-source convention rules  | config, cli, compiler    |      Phase 13 | Generated files read shared paths and conventions from config instead of repeating literals                                                                       | All generators, Vite plugin defaults, diagnostics, docs, and project map stay synchronized from config                        | Production-Ready | CLI and Vite plugin now share `@vanrot/config` loading and diagnostics, including default dev port behavior from normalized config.                        |

## `@vanrot/runtime`

### Reactive Kernel

| Feature              | Package or Area | Planned Phase | Demo-Capable Gate                                         | Production-Ready Gate                                                             | Status           | Notes                                                                                                            |
|----------------------|-----------------|--------------:|-----------------------------------------------------------|-----------------------------------------------------------------------------------|------------------|------------------------------------------------------------------------------------------------------------------|
| Runtime `signal()`   | runtime         |       Phase 2 | Writable signal reads and writes update dependent effects | Edge cases, type docs, disposal interactions, and size budget verified            | Production-Ready | Phase 12B verifies equality behavior, updater behavior, effect notification, and runtime size budget.            |
| Runtime `computed()` | runtime         |       Phase 2 | Derived value updates when source signals change          | Lazy caching, chained computeds, disposal, and graph edge cases verified          | Production-Ready | Phase 12B verifies lazy reads, caching, thrown-computation retry, chained computeds, and untracked reads.        |
| Runtime `effect()`   | runtime         |       Phase 2 | Effects rerun when dependencies change                    | Cleanup, synchronous error behavior, disposal, and batching interactions verified | Production-Ready | Phase 12B verifies cleanup order, rerun errors, dependency replacement, scope disposal, and idempotent disposal. |
| Runtime `batch()`    | runtime         |       Phase 2 | Multiple writes flush once                                | Nested batches and error behavior verified                                        | Production-Ready | Phase 12B verifies nested batches, thrown callbacks, computed invalidation, and outermost flush behavior.        |
| Runtime `untrack()`  | runtime         |       Phase 2 | Reads without subscribing                                 | Nested effect behavior verified                                                   | Production-Ready | Phase 12B verifies untracked reads inside effects and computeds without dependency leaks.                        |

### Lifecycle And Mounting

| Feature                                 | Package or Area | Planned Phase | Demo-Capable Gate                                                                        | Production-Ready Gate                                                                      | Status           | Notes                                                                                                                                    |
|-----------------------------------------|-----------------|--------------:|------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------|------------------|------------------------------------------------------------------------------------------------------------------------------------------|
| Runtime cleanup scopes                  | runtime         |       Phase 2 | Cleanup callbacks run on dispose                                                         | Nested ownership and lifecycle interactions verified                                       | Production-Ready | Phase 12B verifies parent-child ownership, child-before-parent cleanup, repeated disposal, and disposal-time registration behavior.      |
| Runtime `onMount()`                     | runtime         |       Phase 2 | Browser-only callbacks run after insertion                                               | Cleanup return handling and ordering verified                                              | Production-Ready | Phase 12B verifies mount callback flushing, returned cleanup ownership, repeated flush behavior, and cleanup ordering.                   |
| Runtime `onDestroy()`                   | runtime         |       Phase 2 | Cleanup callbacks run on destroy                                                         | Multiple callbacks and nested ownership verified                                           | Production-Ready | Phase 12B verifies cleanup registration, nested ownership, and idempotent disposal through cleanup scopes and mounting.                  |
| Runtime `mount()`                       | runtime         |       Phase 2 | Root component can be constructed and destroyed                                          | Final compiler component contract is integrated                                            | Production-Ready | Phase 12B verifies constructor mounting, root cleanup scope ownership, mount callback flushing, DOM removal, and repeated destroy calls. |
| Runtime compiled component mount bridge | runtime         |       Phase 4 | `mount(App, target)` accepts Vite-transformed component modules and appends returned DOM | Lifecycle ordering, cleanup, destroy idempotence, and compiler output integration verified | Production-Ready | Phase 12B verifies compiled module mounting, scoped effects, listener cleanup, DOM removal, and destroy idempotence.                     |

### Internal Compiler Helpers

| Feature                     | Package or Area | Planned Phase | Demo-Capable Gate                                 | Production-Ready Gate                                                  | Status           | Notes                                                                                                                 |
|-----------------------------|-----------------|--------------:|---------------------------------------------------|------------------------------------------------------------------------|------------------|-----------------------------------------------------------------------------------------------------------------------|
| Runtime internal `listen()` | runtime         |       Phase 2 | Event listener auto-removes through cleanup scope | Options, teardown idempotence, and generated-code integration verified | Production-Ready | Phase 12B verifies event object delivery, listener options, manual teardown, cleanup-scope teardown, and idempotence. |

## `@vanrot/behavior`

### Optional Headless Behavior

| Feature                   | Package or Area   | Planned Phase | Demo-Capable Gate                                                                                              | Production-Ready Gate                                                                                                                  | Status           | Notes                                                                                                        |
|---------------------------|-------------------|--------------:|----------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------|------------------|--------------------------------------------------------------------------------------------------------------|
| Behavior package boundary | behavior, runtime |     Phase 16H, Phase 28 | Existing form, table, overlay, tabs, toast, tooltip, command menu, positioned-layer, and expanded interaction helpers stay out of runtime | Runtime no longer exports behavior helpers; behavior package exports subpaths and retains moved and expanded controller coverage | Production-Ready | Keeps `@vanrot/runtime` focused on signals, lifecycle, and mounting while apps opt into behavior explicitly. |
| Behavior subpath imports  | behavior          |     Phase 16H, Phase 28 | Apps can import `@vanrot/behavior/<name>` for the helper family they selected | Export tests, typecheck, docs, and AI bundle teach form, table, overlay, tabs, tooltip, toast, command-menu, positioned-layer, collapsible, selection, menu, toggle, scroll-area, portal, focus, calendar, drag-drop, table-resize, and all | Production-Ready | `@vanrot/behavior/all` remains a convenience import with no runtime size cap because it is explicit opt-in.  |

## `@vanrot/formatters`

### Formatter Helpers And Registry

| Feature                          | Package or Area       | Planned Phase | Demo-Capable Gate                                                                 | Production-Ready Gate                                                                                   | Status           | Notes                                                                                                                                      |
|----------------------------------|-----------------------|--------------:|-----------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------|------------------|--------------------------------------------------------------------------------------------------------------------------------------------|
| Built-in formatter suite         | formatters            |      Phase 29 | Text, date/time, number, currency, plural, mask, and message helpers format values | Edge cases, context defaults, docs, examples, and package exports verified                              | Production-Ready | Covers `uppercase`, `lowercase`, `titlecase`, `sentencecase`, `truncate`, `fallback`, `initials`, `date`, `time`, `datetime`, `relativeTime`, `duration`, `number`, `currency`, `percent`, `compact`, `filesize`, `join`, `count`, `plural`, `mask`, `message`, and `messages`. |
| Formatter registry execution     | formatters, compiler  |      Phase 29 | Generated output can apply a pipe chain through a small registry helper            | Chaining, custom pipes, presets, diagnostics, browser output, server output, and type coverage verified | Production-Ready | `applyVanrotPipeChain` and `createPipeRegistry` are framework helpers used by compiler output, not application logic hidden in HTML.       |
| Custom pipes and named presets   | formatters, vite-plugin |    Phase 29 | `.pipe.ts` files can export `definePipe`, `enumPipe`, `datePattern`, `numberPattern`, and `maskPattern` definitions | Duplicate detection, async rejection, enum labels, namespace variants, docs, and examples verified       | Production-Ready | Custom role files keep domain formatting close to source while staying discoverable by Vite, future Forge, editor tooling, and AI reports. |
| Formatter testing helper         | formatters            |      Phase 29 | Custom pipe handlers can be exercised without compiling a full template            | Public `@vanrot/formatters/testing` export, focused tests, docs snippet, and package metadata verified  | Production-Ready | `createPipeTest` gives teams a narrow unit-test path for backend enum display labels and preset behavior.                                  |

## `@vanrot/compiler`

### Component File Model

| Feature                            | Package or Area          | Planned Phase | Demo-Capable Gate                                                              | Production-Ready Gate                                                                                                                                            | Status           | Notes                                                                                                                                                                     |
|------------------------------------|--------------------------|--------------:|--------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Compiler file convention resolver  | compiler                 |       Phase 3 | Resolves `.component.ts`, `.component.html`, `.component.css` siblings         | Supports all Vanrot UI role suffixes with diagnostics                                                                                                            | Production-Ready | Phase 12C verifies `.component`, `.page`, and `.button` role triplets with source-rich diagnostics; broader future UI role names remain Phase 16 config/generator work.   |
| Compiler component class detection | compiler                 |       Phase 3 | Matching named export class is found                                           | Multiple exports, default exports, inheritance, and constructor diagnostics verified                                                                             | Production-Ready | Phase 12C verifies named exports, default-export rejection, multiple-candidate diagnostics, and required-constructor diagnostics.                                         |
| Component module TypeScript typing | vite-plugin and compiler |      Phase 12 | Demo apps can typecheck with an explicit transformed-component import boundary | First-class TypeScript declarations or plugin-aware typing remove user-facing suppressions for transformed `.component.ts`, `.page.ts`, and `.button.ts` modules | Production-Ready | Phase 12E exports metadata-derived named compiled modules from Vite, widens router pages to `ComponentType`, removes app-facing suppressions, and verifies the 12E audit. |

### Template Compilation

| Feature                                | Package or Area    | Planned Phase | Demo-Capable Gate                                                                       | Production-Ready Gate                                                                      | Status           | Notes                                                                                                                                                                                    |
|----------------------------------------|--------------------|--------------:|-----------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------|------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Compiler text interpolation `{{ }}`    | compiler           |       Phase 3 | Basic expressions update generated text nodes through `effect()`                        | Rich diagnostics, escaping rules, source maps, and nested context behavior verified        | Production-Ready | Phase 12C verifies source locations, malformed-expression diagnostics, nested local context, and mapping metadata.                                                                       |
| Compiler event binding `(event)`       | compiler           |       Phase 3 | `(click)="increment()"` calls a component method through `listen()`                     | Typed events, arguments, modifiers, accessibility diagnostics, and cleanup tests verified  | Production-Ready | Phase 12C verifies method-only event policy, code frames, docs links, cleanup through `listen()`, and generated UI/button integration.                                                   |
| Compiler property binding `[property]` | compiler           |       Phase 3 | `[value]="name()"` and `[disabled]="saving()"` assign DOM properties through `effect()` | Property vs attribute matrix, boolean behavior, SVG behavior, and diagnostics verified     | Production-Ready | Phase 12C verifies binding effects, invalid-expression diagnostics, nested generated output, and source mapping metadata.                                                                |
| Compiler expression rewriting          | compiler           |       Phase 3 | Bare identifiers rewrite to `ctx.<name>` without `eval()`                               | Full expression grammar policy, diagnostics, and source locations verified                 | Production-Ready | Phase 12C verifies the approved grammar subset, local loop signals, globals, diagnostics, and source locations.                                                                          |
| Compiler `@if` conditionals            | compiler           |      Phase 12 | Conditional DOM can render in a demo                                                    | Nested cleanup, keyed transitions, diagnostics, and source maps verified                   | Production-Ready | Phase 12C verifies `@if`/`@else` parsing, nested cleanup scopes, generated output, source mappings, and production fixture coverage.                                                     |
| Compiler `@for` loops                  | compiler           |      Phase 12 | List DOM can render in a demo                                                           | Keyed diffing, cleanup, nested blocks, and diagnostics verified                            | Production-Ready | Phase 12C verifies keyed loops, `@empty`, local signal context, cleanup, nested control flow, and production fixture coverage.                                                           |
| Compiler `@switch` blocks              | compiler           |      Phase 24 | Status-based template branches can be expressed without nested `@if` blocks             | Case matching, default handling, diagnostics, source maps, and readability rules verified  | Deferred         | Future compiler syntax. Phase 12C owns only `@if`, `@else`, `@for`, and `@empty`.                                                                                                        |
| Compiler `@await` blocks               | compiler           |      Phase 24 | Async template states can express loading, success, and failure branches                | Cancellation, error boundaries, diagnostics, SSR behavior, and source maps verified        | Deferred         | Future compiler syntax that should be designed with async resources instead of squeezed into Phase 12C.                                                                                  |
| Compiler `@defer` blocks               | compiler           |      Phase 24 | Deferred content can be declared in templates                                           | Loading triggers, placeholders, chunking, hydration, diagnostics, and source maps verified | Deferred         | Future compiler syntax that should be designed with performance, SSR, and build integration.                                                                                             |
| Compiler child components              | compiler           |      Phase 12 | Parent can instantiate one child component                                              | Props, lifecycle boundaries, cleanup, slots, and diagnostics verified                      | Production-Ready | Phase 12C verifies child dependency metadata, initial input handoff before required-input reads, reactive parent input updates, invalid input diagnostics, and projected slot fragments. |
| Compiler slots                         | compiler           |      Phase 12 | Parent can pass static slot content                                                     | Named slots, fallback content, scope rules, and diagnostics verified                       | Production-Ready | Phase 12C verifies `<slot>`, `<slot.name>`, `slot.name` projection, fallback rendering, projected content grouping, and integration fixture coverage.                                    |
| Compiler template pipes                | compiler, formatters, vite-plugin | Phase 29 | Interpolation can lower `{{ value | pipeName }}`, arguments, namespace variants, and chained pipe calls | Source spans, diagnostics, browser output, server output, custom registry imports, docs, examples, and release coverage verified | Production-Ready | Phase 29 keeps pipes compiler-owned and runtime-neutral while generated output imports `@vanrot/formatters` only when templates use pipes. |
| Compiler two-way binding               | compiler and forms |      Phase 21 | A simple input can read and write a signal                                              | Forms integration, event policy, validation, and diagnostics verified                      | Deferred         | `@vanrot/forms` shipped in Phase 21; compiler two-way binding syntax remains deferred until a forms-aware syntax is designed.                                                            |

### Styling

| Feature             | Package or Area | Planned Phase | Demo-Capable Gate                                        | Production-Ready Gate                                                        | Status           | Notes                                                                                                                                |
|---------------------|-----------------|--------------:|----------------------------------------------------------|------------------------------------------------------------------------------|------------------|--------------------------------------------------------------------------------------------------------------------------------------|
| Compiler scoped CSS | compiler        |       Phase 3 | Basic selectors rewrite with a generated scope attribute | Complex selectors, global escapes, source maps, and CSS diagnostics verified | Production-Ready | Phase 12C verifies `:host`, `:global(...)`, pseudo classes, media rules, unsupported selector diagnostics, and CSS mapping metadata. |

### Diagnostics And Inspection

| Feature                            | Package or Area  | Planned Phase | Demo-Capable Gate                                    | Production-Ready Gate                                                                               | Status           | Notes                                                                                                                                                                            |
|------------------------------------|------------------|--------------:|------------------------------------------------------|-----------------------------------------------------------------------------------------------------|------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Compiler readable generated output | compiler         |       Phase 3 | Generated code is snapshot-tested and understandable | Stable output, source maps, and docs integration verified                                           | Production-Ready | Phase 12C keeps generated output readable across bindings, control flow, children, slots, and mappings; command-facing project inspection now lives under `vr doctor --inspect`. |
| Compiler i18n extraction           | compiler and cli |      Phase 12 | Template keys can be extracted into locale JSON      | Missing-key diagnostics, pluralization policy, and CLI workflow verified                            | Deferred         | Excluded from Phase 3.                                                                                                                                                           |
| Compiler source maps               | compiler         |      Phase 12 | Generated output maps to template and CSS files      | Accurate template, style, and generated-code mappings verified                                      | Production-Ready | Phase 12C returns source-map-ready JS and CSS mapping metadata; packaged `.map` file emission remains tied to Vite/build hardening.                                              |
| Compiler production diagnostics    | compiler and cli |      Phase 12 | Basic fatal diagnostics identify unsupported syntax  | Code frames, suggestions, recovery, documentation links, and integration with Vite and CLI verified | Production-Ready | Phase 12C verifies diagnostic catalog entries, code frames, source text, suggestions, docs paths, and compiler audit coverage.                                                   |
| Compiler pipe diagnostics          | compiler, vite-plugin, formatters | Phase 29 | Unknown pipes and invalid pipe arguments produce fatal diagnostics | Unknown variants, duplicate pipe names, duplicate presets, async definitions, code frames, line/column output, and docs links verified | Production-Ready | Pipe diagnostics use stable `VR_PIPE_*` codes and route developers to `/docs/formatters`.                                                                                        |

### Vite-Aware Compiler Hooks

| Feature                       | Package or Area | Planned Phase | Demo-Capable Gate                                                                                   | Production-Ready Gate                                                                                       | Status       | Notes                                                                                                   |
|-------------------------------|-----------------|--------------:|-----------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------|--------------|---------------------------------------------------------------------------------------------------------|
| Compiler Vite import override | compiler        |       Phase 4 | Compiler can emit generated code that imports the component class from a Vite virtual source module | Option is documented, tested with Vite integration, and does not change normal compiler output when omitted | Demo-Capable | Phase 4 verified the narrow override with compiler tests and Vite fixture builds; not production-ready. |

## `@vanrot/vite-plugin`

### Plugin API

| Feature                                | Package or Area     | Planned Phase | Demo-Capable Gate                                           | Production-Ready Gate                                                                           | Status           | Notes                                                                                                                                              |
|----------------------------------------|---------------------|--------------:|-------------------------------------------------------------|-------------------------------------------------------------------------------------------------|------------------|----------------------------------------------------------------------------------------------------------------------------------------------------|
| Vite plugin public API                 | vite-plugin         |       Phase 4 | `vanrot()` returns a Vite plugin usable in `vite.config.ts` | Typed options, peer dependency behavior, docs, and integration examples verified                | Production-Ready | Default export plus named `vanrotPlugin` alias are covered by package tests and clean fixture builds.                                              |
| Vite config loading `vanrot.config.ts` | vite-plugin and cli |      Phase 13 | Plugin can read Vanrot config defaults                      | Config discovery, schema validation, CLI sharing, dev-server defaults, and diagnostics verified | Production-Ready | `@vanrot/vite-plugin` and `@vanrot/cli` now load shared normalized config through `@vanrot/config`, including consistent diagnostics and defaults. |

### Transform And Virtual Modules

| Feature                     | Package or Area | Planned Phase | Demo-Capable Gate                                                                  | Production-Ready Gate                                                                        | Status           | Notes                                                                                           |
|-----------------------------|-----------------|--------------:|------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------|------------------|-------------------------------------------------------------------------------------------------|
| Vite transform integration  | vite-plugin     |       Phase 4 | Vanrot component compiles through Vite dev/build                                   | Watch mode, rebuilds, HMR preparation, and error overlay verified                            | Production-Ready | Phase 12D verifies transforms, JS sourcemaps, dev invalidation, and production fixture builds.  |
| Vite virtual source modules | vite-plugin     |       Phase 4 | Generated code imports original component class through an internal virtual module | Recursive import prevention, path encoding, Vite resolution, and build/dev behavior verified | Production-Ready | Virtual source IDs avoid recursive `.component.ts` transforms in dev and build.                 |
| Vite CSS virtual modules    | vite-plugin     |       Phase 4 | Generated component modules import scoped CSS through Vite                         | CSS transforms, dev injection, production extraction, and cache invalidation verified        | Production-Ready | Phase 12D verifies cached virtual CSS, CSS maps, extraction, and package-output fixture builds. |
| Vite `.pipe.ts` discovery   | vite-plugin, compiler, formatters | Phase 29 | Plugin discovers app-global pipe files and feeds metadata to compiler options      | Startup/rebuild diagnostics, watched pipe files, duplicate detection, async rejection, docs, and tests verified | Production-Ready | Vite surfaces pipe errors in the terminal with file path, line, column, code, code frame, suggestion, and docs path. |

### Dev Server And Production Build

| Feature                           | Package or Area | Planned Phase | Demo-Capable Gate                                              | Production-Ready Gate                                                                             | Status           | Notes                                                                                                    |
|-----------------------------------|-----------------|--------------:|----------------------------------------------------------------|---------------------------------------------------------------------------------------------------|------------------|----------------------------------------------------------------------------------------------------------|
| Vite file watching                | vite-plugin     |       Phase 4 | Changes to TS, HTML, and CSS trigger rebuilds                  | Dependency graph accuracy and HMR behavior verified                                               | Production-Ready | Sibling HTML/CSS files invalidate owner modules and keep full reload only as the missing-owner fallback. |
| Vite diagnostics overlay bridge   | vite-plugin     |       Phase 4 | Compiler diagnostics appear as Vite errors or warnings         | Code frames, source maps, suggestions, and docs links verified                                    | Production-Ready | Compiler diagnostics include code frames, suggestions, and docs links in Vite errors/warnings.           |
| Vite production build integration | vite-plugin     |       Phase 4 | `vite build` emits JavaScript and CSS for a Vanrot fixture app | Asset output, minification compatibility, sourcemap policy, and framework examples verified       | Production-Ready | Clean package-output fixture builds JavaScript, CSS, JS maps, and CSS maps through Vite.                 |
| Vite true HMR                     | vite-plugin     |      Phase 12 | Component HTML and CSS can update without full page reload     | State preservation, partial invalidation, cleanup safety, and compiler boundary tracking verified | Production-Ready | Phase 12D returns owner modules for Vite HMR; generated accept/dispose hooks can be revisited if needed. |

## `@vanrot/cli` *

*The CLI needs a dedicated design-focused brainstorming cycle before production polish. The target is beautiful yet
functional: calm, premium, fast to read, useful under pressure, and not decorative at the cost of clarity.*

### Project Creation And Generation

| Feature                     | Package or Area | Planned Phase | Demo-Capable Gate                                                                        | Production-Ready Gate                                                                                                 | Status                         | Notes                                                                                                                                        |
|-----------------------------|-----------------|--------------:|------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------|--------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------|
| CLI `vr create`             | cli             |       Phase 5 | Creates a routed Vanrot app in standalone-style mode and internal fixture/workspace mode | Package manager handling, template variants, overwrite safety, optional behavior selection, errors, and docs verified | Demo-Capable through Phase 16H | Phase 16H adds `--behavior` and `--no-behavior` so generated apps can opt into `@vanrot/behavior` independently from UI and router defaults. |
| CLI `vr generate component` | cli             |       Phase 5 | Creates convention component files                                                       | Role suffixes, folder rules, naming diagnostics, and tests verified                                                   | Demo-Capable                   | Phase 5 verified component generation in feature and default folders.                                                                        |
| CLI `vr generate page`      | cli             |       Phase 5 | Creates convention page files                                                            | Routing integration and feature-folder placement verified                                                             | Demo-Capable                   | Phase 5 verified page generation in feature and default folders without router integration.                                                  |

### Command Wrappers

| Feature                           | Package or Area          | Planned Phase | Demo-Capable Gate                                                                           | Production-Ready Gate                                                                                                                 | Status           | Notes                                                                                                                                                                                              |
|-----------------------------------|--------------------------|--------------:|---------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------|------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| CLI `vr dev`                      | cli                      |       Phase 5 | Invokes the Vite dev server for a Vanrot app                                                | Config loading, diagnostics, dev server lifecycle, and readable startup output verified                                               | Demo-Capable     | Phase 5 verifies the thin runner wrapper through injected runner tests; production lifecycle polish remains future work.                                                                           |
| CLI `vr dev` localhost experience | cli and web              |      Phase 14 | `vr dev` opens or serves a useful local Vanrot experience for new apps                      | Browser landing experience, terminal output, errors, route awareness, docs links, accessibility, and cross-platform behavior verified | Deferred         | Vanrot should eventually have an `ng serve`-quality local development experience without becoming heavy. The preferred default local port is `1010` if design and compatibility checks approve it. |
| CLI configurable dev server port  | cli, config, vite-plugin |      Phase 13 | `vanrot.config.ts` can set `devServer.port`, defaulting to `1010` for generated Vanrot apps | Vite `server.port` and `strictPort` integration, env overrides, collision handling, terminal output, tests, and docs verified         | Production-Ready | `vr dev` now reads normalized config and forwards `--port` from `devServer.port`, defaulting to `1010` when not configured.                                                                        |
| CLI `vr build`                    | cli                      |       Phase 5 | Invokes Vite build for a Vanrot app                                                         | Config loading, diagnostics, and build report verified                                                                                | Demo-Capable     | Phase 8 verified direct CLI builds can resolve app-local binaries from `node_modules/.bin`. Build reports remain deferred.                                                                         |
| CLI `vr test`                     | cli                      |       Phase 5 | Invokes app tests                                                                           | Testing package integration and generated test support verified                                                                       | Demo-Capable     | Phase 5 verifies the thin test wrapper through injected runner tests before `@vanrot/testing` exists.                                                                                              |

### Doctor, Reports, And Maintenance

| Feature                                | Package or Area       | Planned Phase | Demo-Capable Gate                                                                                          | Production-Ready Gate                                                                                                                 | Status                            | Notes                                                                                                                              |
|----------------------------------------|-----------------------|--------------:|------------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------|------------------------------------------------------------------------------------------------------------------------------------|
| CLI `vr doctor` starter checks         | cli                   |       Phase 5 | Runs project-health checks and starter Vanrot rule checks                                                  | Diagnostics for naming, i18n, a11y, cleanup, imports, and structure verified                                                          | Demo-Capable                      | Phase 5 verifies project health, sibling files, role suffixes, raw visible template text, and nested `if` warnings only.           |
| CLI `vr remove behavior`               | cli and behavior      |     Phase 16H | Optional behavior helpers can be removed from config and package metadata                                  | Idempotent config edits, package pruning, command help, docs, and tests verified                                                      | Production-Ready                  | Supports selected helper cleanup and `--all` package cleanup without touching unrelated project files.                             |
| CLI behavior doctor checks             | cli and behavior      |     Phase 16H | `vr doctor` can see behavior config/import/package drift                                                   | Unused installs, configured-but-unimported helpers, undeclared imports, stale runtime imports, root imports, docs, and tests verified | Production-Ready                  | Uses stable `VRTB###` warning codes in doctor messages.                                                                            |
| CLI full `vr doctor` diagnostics       | cli                   |      Phase 14 | Starter checks can report health and first Vanrot rules                                                    | Full a11y, i18n, SSR, import-depth, unused CSS/components, cleanup, code frames, docs links, and strict mode verified                 | Deferred                          | Phase 5 intentionally covers only project health plus the starter Vanrot rule set.                                                 |
| CLI strict `vr doctor --strict` mode   | cli                   |      Phase 14 | Strict mode can promote selected warnings to errors in local and CI workflows                              | Rule configuration, CI exit behavior, docs links, suppression policy, and team adoption workflow verified                             | Deferred                          | Phase 7 does not implement strict diagnostics; it only prepares project intelligence files that future diagnostics can consume.    |
| CLI i18n diagnostics                   | cli and compiler      |      Phase 14 | Templates can report raw user-facing text and missing i18n-ready patterns                                  | Key extraction, missing-key checks, locale coverage, pluralization policy, code frames, and docs verified                             | Deferred                          | Phase 7 keeps i18n as map metadata only. Full i18n diagnostics need their own design.                                              |
| CLI accessibility diagnostics          | cli and compiler      |      Phase 14 | Templates can report common accessibility issues                                                           | Keyboard interaction checks, ARIA guidance, semantic element checks, focus-management rules, code frames, and docs verified           | Deferred                          | Phase 7 does not inspect template accessibility. This remains production diagnostic work.                                          |
| CLI build reports and budgets          | cli                   |      Phase 14 | `vr build` can invoke Vite build                                                                           | Runtime/app/CSS budgets, largest page report, unused CSS summary, warning policy, and CI behavior verified                            | Deferred                          | Kept out of Phase 5 so the CLI MVP stays demo-capable.                                                                             |
| CLI `vr doctor --inspect`              | cli and intelligence  |      Phase 23 | `vr doctor --inspect` includes a read-only role, graph, route, i18n, and stale-state summary               | Richer diagnostics can connect project-map facts to code frames and docs links once full doctor diagnostics ship                      | Demo-Capable                      | Replaces the standalone `vr inspect` command idea so users have one project-understanding doorway.                                 |
| CLI cache cleaning command             | cli                   |      Phase 18 | `vr cache clean` removes allowlisted Vanrot-owned generated metadata with a clear summary and dry-run mode | Expanded cache location policy, cross-platform cleanup, and richer doctor integration verified                                        | Demo-Capable                      | Uses `clean`, not `clear`; current cleanup boundary is `.vanrot/cache` and `.vanrot/project-map.json`.                             |
| Project map `.vanrot/project-map.json` | cli                   |       Phase 7 | Maps role-based files, source root, i18n file hints, schema version, and generated timestamp               | Incremental updates, route graphing, dependency graphing, compiler-aware metadata, and diagnostics verified                           | Demo-Capable                      | Phase 7 verified the demo-capable filesystem convention map, not the production graph engine.                                      |
| Project route and dependency graphing  | cli, compiler, router |      Phase 23 | Project intelligence can show simple relationships between known role files                                | Route graph, component role graph, import graph, graph output formats, stale graph detection, and docs verified                       | Production-Ready through Phase 23 | Phase 23 upgrades `vr map` to schema v2 graph metadata while keeping deterministic local filesystem inputs.                        |
| Compiler-aware project intelligence    | cli and compiler      |      Phase 23 | Project map can incorporate compiler metadata beyond filenames                                             | Role, template, style, source-location, and graph manifest metadata verified without requiring compiler rewrites                      | Production-Ready through Phase 23 | Phase 23 exposes the compiler-aware metadata currently available and leaves deeper diagnostics/source-map analysis for later work. |
| AI rules `.vanrot/ai-rules.md`         | cli                   |       Phase 7 | Generates provider-neutral Vanrot rules file                                                               | Configured sections, custom sections, validation diagnostics, manifest metadata, and provider-independent workflows verified          | Production-Ready through Phase 23 | Phase 23 makes AI rules configurable; Phase 25 keeps canonical AI consumption provider-neutral.                                    |

### Terminal Experience And Inspection

| Feature                            | Package or Area       | Planned Phase | Demo-Capable Gate                                                                                            | Production-Ready Gate                                                                                                            | Status                            | Notes                                                                                                                                    |
|------------------------------------|-----------------------|--------------:|--------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------|-----------------------------------|------------------------------------------------------------------------------------------------------------------------------------------|
| CLI Quiet Premium reporter         | cli                   |       Phase 5 | Commands use a shared structured reporter with calm headings, status lines, warnings, errors, and next steps | Cross-platform terminal behavior, interactive states, theming, accessibility, snapshots, and docs verified                       | Production-Ready                  | Phase 14 aligns reporter labels, continuation columns, headings, and next-step output across memory and console reporters.               |
| CLI design language *              | cli                   |      Phase 14 | CLI design principles are approved for command layout, rhythm, progress, errors, and next steps              | Real command journeys, terminal snapshots, narrow/wide viewport checks, accessibility, cross-platform QA, and docs verified      | Demo-Capable                      | Phase 14 adds grouped root help, command descriptions, examples, stable status labels, and focused tests.                                |
| CLI production terminal experience | cli                   |      Phase 14 | Reporter foundation can render consistent command summaries                                                  | Claude Code-quality polish with refined interaction, progress states, terminal resizing, theming, and cross-platform QA verified | Demo-Capable                      | Phase 14 covers help, reporter alignment, doctor shape, structured output flags, and CLI diagnostic-code stability.                      |
| CLI inspection doorway             | cli and intelligence  |      Phase 23 | Project inspection lives under `vr doctor --inspect` instead of a standalone command                         | Future richer inspection should extend doctor output or documented child sections before adding another top-level command        | Demo-Capable                      | Standalone `vr inspect` is intentionally not part of the command surface.                                                                |
| Designed starter welcome page      | cli, router, ui, docs |      Phase 14 | A new Vanrot app has a polished localhost page that demonstrates routes and framework feel                   | Visual design, accessibility, responsive behavior, route links, docs links, screenshots, and update workflow verified            | Deferred                          | Requested as the Vanrot equivalent of the first `ng serve` impression. Keep it light and framework-owned.                                |
| Optional AI commands               | cli                   |      Phase 25 | Provider-neutral AI command can build, verify, inspect, and maintain framework AI knowledge when configured  | Bundle build/verify/doctor/MCP plus context, prompt, history, and summary doorway files verified without provider secrets        | Production-Ready through Phase 25 | Provider-specific OpenAI, Claude, Ollama, or self-hosted adapters remain future extensions; the canonical source is the verified bundle. |

## `@vanrot/store` *

*Requires a dedicated brainstorming and design cycle before implementation. The goal is not to piggyback on NgRx or
recreate an Angular-era, RxJS-heavy architecture. Vanrot store should be future-facing: signal-native,
enterprise-capable, lightweight by default, and written with clear English-first APIs. Redux, NgRx, and RxJS are
references for enterprise needs and migration pressure, not the design destination.*

### Enterprise Store Core

| Feature                    | Package or Area | Planned Phase | Demo-Capable Gate                                                          | Production-Ready Gate                                                                                | Status   | Notes                                                                                                                                                   |
|----------------------------|-----------------|--------------:|----------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------|----------|---------------------------------------------------------------------------------------------------------------------------------------------------------|
| Store package foundation * | store           |      Phase 19 | `@vanrot/store` can be installed without increasing `@vanrot/runtime` size | Package exports, docs, examples, tree-shaking, SSR boundaries, and release checks verified           | Production-Ready | Optional enterprise package ships with docs, AI docs, examples, package tests, and a separate runtime-plus-store size budget.                         |
| Store state containers     | store           |      Phase 19 | A store owns typed state and exposes signal-native reads                   | Immutable update policy, nested state ergonomics, reset/replace behavior, and serialization verified | Production-Ready | `defineState` owns typed signal-native state with set, patch, reset, and current reads.                                                                |
| Store actions              | store           |      Phase 19 | Typed actions can be declared and dispatched                               | Payload typing, namespacing, fluent action sets, and type metadata verified                         | Production-Ready | `actionSet().start().success().error()` gives enterprise command/event flow without handwritten action strings.                                       |
| Store reducers             | store           |      Phase 19 | Reducers update state from actions                                         | Immutable patch/set behavior, action matching, and test helpers verified                            | Production-Ready | `defineReducer(state).on(action).patch(fn)` and `.set(fn)` keep reducer code readable without NgRx boilerplate.                                      |
| Store selectors            | store           |      Phase 19 | Selectors expose derived signal values                                     | Parameterized selectors and type inference verified                                                | Production-Ready | `defineSelectors(state).selectorName(fn)` avoids string selector keys and bridges cleanly to page fields.                                            |
| Store effects              | store           |      Phase 19 | Effects can react to actions and dispatch follow-up actions                | Success/error mapping, `latestBy`, `skipWhen`, `cancelWhen`, timeout, retry, trace, and stale-write cancellation verified | Production-Ready | Fluent effects cover the common enterprise side-effect stack; Phase 20 adds headless lifecycle inspection on top.                                   |

### Enterprise Store Hardening

| Feature                         | Package or Area | Planned Phase | Demo-Capable Gate                                                                   | Production-Ready Gate                                                                                      | Status   | Notes                                                                                                                                              |
|---------------------------------|-----------------|--------------:|-------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------|----------|----------------------------------------------------------------------------------------------------------------------------------------------------|
| Store inspection protocol       | store           |      Phase 20 | A store instance can expose a headless action/state/effect timeline                  | `inspectStore`, `StoreInspector`, bounded history, observer unsubscribe, observer failure isolation, and export contracts verified | Production-Ready | Uses `vanrot.store.inspection.v1` so future devtools can consume store events without putting UI inside `@vanrot/store`.                          |
| Store snapshots and replay      | store           |      Phase 20 | Inspection can capture state snapshots and replay recorded actions                   | Manual snapshots, automatic dispatch snapshots, reducer-backed replay steps, missing-snapshot failures, and changed-key reporting verified | Production-Ready | Replay runs through an internal inspection controller so public store instances do not leak reducer internals.                                     |
| Store effect lifecycle tracing  | store           |      Phase 20 | Effects emit inspectable start, success, retry, timeout, failure, cancellation events | `latestBy`, `cancelWhen`, retry, timeout, stale-write prevention, trace names, concurrency keys, and cancellation reasons verified     | Production-Ready | Keeps async store behavior explainable without teaching new programmers RxJS streams.                                                              |
| Store hardening docs and AI     | docs, ai        |      Phase 20 | Store hardening has real docs child routes and refreshed AI bundle entries           | `/docs/store/inspection`, `/docs/store/replay`, sidebar children, generated AI docs, example coverage, and site verification pass        | Production-Ready | Store hardening docs stay package-owned; future `@vanrot/devtools` panel work is tracked separately in `future-pipeline.md`.                      |

### Redux Compatibility

| Feature                  | Package or Area | Planned Phase | Demo-Capable Gate                                                        | Production-Ready Gate                                                                                   | Status   | Notes                                                                                    |
|--------------------------|-----------------|--------------:|--------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------|----------|------------------------------------------------------------------------------------------|
| Redux-style interop      | store           |        Future | Vanrot store can document migration from Redux-style mental models        | Migration docs, adapter tests, and middleware policy verified without making Redux a required dependency | Deferred | Not part of Phase 20. Redux remains an external reference point, not a store dependency or design destination.       |
| Devtools and time travel | devtools, store |        Future | Store changes can be rendered in the existing `@vanrot/devtools` shell    | Browser devtools integration, store panel UI, time-travel controls, redaction, and production stripping verified | Deferred | Phase 20 ships the headless inspection/replay contract; `@vanrot/devtools` repair and Store panel remain future pipeline work. |

### RxJS Interop

| Feature                    | Package or Area | Planned Phase | Demo-Capable Gate                                                              | Production-Ready Gate                                                                                    | Status   | Notes                                                                                       |
|----------------------------|-----------------|--------------:|--------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------|----------|---------------------------------------------------------------------------------------------|
| RxJS bridge                | store/rxjs      |      Phase 20 | Actions, selectors, or signals can interoperate with RxJS observables          | Optional peer dependency, observable cleanup, scheduler behavior, backpressure policy, and docs verified | Deferred | Required for enterprise teams that rely on RxJS, but kept out of the default store path.    |
| Observable effects adapter | store/rxjs      |      Phase 20 | Store effects can be authored with observable pipelines when RxJS is installed | Cancellation modes, typed action streams, error handling, marble tests, and cleanup verified             | Deferred | Provides an NgRx-style escape hatch without making Vanrot store feel RxJS-heavy by default. |

## `@vanrot/router`

### Routing

| Feature                                          | Package or Area                    | Planned Phase | Demo-Capable Gate                                                                                                            | Production-Ready Gate                                                                                                                        | Status                                    | Notes                                                                                                                                                                                                                                    |
|--------------------------------------------------|------------------------------------|--------------:|------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------|-------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Router named route config                        | router                             |       Phase 8 | `defineRoutes()` stores named routes from `src/routes.ts` as the single route source of truth                                | Type inference, duplicate route detection, diagnostics, docs, and migration guidance verified                                                | Demo-Capable                              | Phase 8 verified named route ownership in router tests and the generated starter.                                                                                                                                                        |
| Router route outlet `<vr-router>`                | router and compiler                |       Phase 8 | `<vr-router>` renders the matched route page                                                                                 | Nested outlets, layout boundaries, error outlets, SSR behavior, and diagnostics verified                                                     | Demo-Capable                              | Phase 8 verified compiler lowering, DOM mounting, lazy-page swapping, and fixture builds.                                                                                                                                                |
| Router named route primitive `<vr route.name />` | router and compiler                |       Phase 8 | Named route links render accessible anchors from route metadata                                                              | Active state, custom labels, params, keyboard/a11y audits, and diagnostics verified                                                          | Demo-Capable                              | Phase 8 verified links render from route metadata without repeating path or label strings in templates.                                                                                                                                  |
| Router route params                              | router                             |       Phase 8 | `/users/:id` style routes populate `routeParams()`                                                                           | Typed params, param validation, optional params, encoded segments, and docs verified                                                         | Demo-Capable                              | Phase 8 verified path param extraction and signal updates; typed param-link generation remains deferred.                                                                                                                                 |
| Router lazy `loadPage`                           | router and vite-plugin             |       Phase 8 | Non-root pages can lazy-load through `loadPage` and Vite build output                                                        | Error boundaries, preloading, chunk naming, loading states, and production diagnostics verified                                              | Demo-Capable                              | Phase 8 verified lazy page resolution and Vite code-split output in fixture and generated-app builds.                                                                                                                                    |
| Router `.page.ts` compilation                    | compiler and vite-plugin           |       Phase 8 | `.page.ts` files compile through the same contract as `.component.ts` files                                                  | Role diagnostics, TS typing, generated output stability, source maps, and docs verified                                                      | Demo-Capable                              | Phase 8 verified `.page.ts` role resolution, transforms, sibling template/style watching, and build output.                                                                                                                              |
| Router route contract builder refs               | router                             |     Phase 15A | Route definitions can be authored from one `src/routes.ts` source of truth                                                   | Parent route refs, normalized child paths, object-form compatibility, diagnostics, and tests verified                                        | Production-Ready                          | Phase 15A added `createRoutes()`, route refs, parent normalization, and missing-render-target diagnostics without route-name strings.                                                                                                    |
| Router query string API                          | router                             |     Phase 15A | Query strings can be read without breaking path params                                                                       | Typed query values, encoding, defaults, array values, history behavior, and docs verified                                                    | Production-Ready                          | Phase 15A added query metadata, URL serialization, parsed query matching, defaults, arrays, and query-preserving navigation.                                                                                                             |
| Router nested routes and layouts                 | router, compiler, cli, vite-plugin |     Phase 15B | Parent routes can render child outlets and layout shells                                                                     | Root router, route outlets, index child pages, shared layout retention, lazy child pages, route-owned nav metadata, and diagnostics verified | Production-Ready                          | Phase 15B adds nested layout rendering through one app-level `<vr-router />`, route-local `<vr-outlet />`, index child pages, route-owned nav metadata, shared-layout retention, and diagnostics for invalid parent/child relationships. |
| Router guards                                    | router                             |     Phase 15C | Routes can block, redirect, or allow navigation through explicit guard APIs                                                  | Async guards, cancellation, auth examples, diagnostics, testing helpers, and docs verified                                                   | Production-Ready                          | Phase 15C added `canEnter`, parent-to-child guard execution, guard redirects, blocking, and stale async navigation protection.                                                                                                           |
| Router preloading                                | router                             |     Phase 15D | Lazy pages can be preloaded through a light strategy                                                                         | Route-owned preload policy, user intent heuristics, lazy chain warming, failure diagnostics, and guard/redirect safety verified              | Production-Ready                          | Phase 15D added `routes.preload.intent()`, hover/focus/touch generated link and router-managed internal-anchor preloading, lazy layout/page chain warming, failure diagnostics, and no guard/redirect/history side effects.              |
| Router keepAlive route views                     | router                             |     Phase 15D | Route views can be preserved in memory for same-day back navigation                                                          | Memory-only same-day route view reuse, guard-before-restore behavior, reset cleanup, diagnostics, and integration tests verified             | Production-Ready                          | Phase 15D added `routes.keepAlive.sessionDay()` for local component state preservation without API data caching; async outlet swaps now keep the current view visible until the next view resolves.                                      |
| Router navigation polish                         | router, config                     |     Phase 15E | Route navigation can update optional title/meta, scroll to top, restore back/forward scroll, and move focus after view mount | Configurable title/meta/scroll/focus behavior, metadata diagnostics, guard safety, hash behavior, and integration tests verified             | Production-Ready                          | Phase 15E adds route-owned title/meta, `vanrot.config.ts` polish controls, Vite config injection, hash-safe matching, scroll restoration, and focus after view mount.                                                                    |
| Router active route links                        | router and compiler                |     Phase 15A | Route links expose exact active state from router state                                                                      | `aria-current="page"`, same-origin click handling, params/query href generation, and tests verified                                          | Production-Ready                          | Phase 15A keeps active link behavior framework-owned without user-authored active-class strings.                                                                                                                                         |
| Router custom link labels                        | router and compiler                |      Phase 15 | Route links can override generated labels when needed                                                                        | Accessibility, i18n labels, class/style integration, diagnostics, and docs verified                                                          | Deferred                                  | Route labels remain owned by `src/routes.ts`; custom display policy needs its own design.                                                                                                                                                |
| Router typed param-link generation               | router and compiler                |     Phase 15A | Links to param routes can be generated without manual path strings                                                           | Type-safe params, missing-param diagnostics, encoding, template syntax, and docs verified                                                    | Production-Ready                          | Phase 15A added `buildRouteUrl(...)`, `param.*` and `query.*` compiler lowering, and runtime missing/unknown param checks.                                                                                                               |
| Router route diagnostics foundation              | router                             |     Phase 15A | Route diagnostics have stable codes, messages, suggestions, and docs hooks                                                   | Shared diagnostic factory and code source of truth verified                                                                                  | Production-Ready                          | Phase 15A added reusable `VR_ROUTE_*` diagnostics and route-level diagnostic collection.                                                                                                                                                 |
| Router strict route diagnostics                  | router, compiler, cli              |     Phase 15C | Invalid route refs, duplicate paths, unreachable routes, and CLI route graph issues can be reported                          | Code frames, docs links, strict mode, CI behavior, and route graph integration verified                                                      | Production-Ready for navigation decisions | Phase 15C covers duplicate full paths, redirect misuse, missing targets, redirect loops, invalid guards, invalid guard results, and guard redirect loops.                                                                                |

## `@vanrot/ui` *

*The UI package needs dedicated product/design brainstorming cycles as it matures. Phase 9 owns only the demo-capable
package foundation, token CSS, `vr add button`, typed button primitive files, and compiler-lowered `<vr-button>`. The
package starts with the October production flavor, based on a shadcn-inspired developer-owned design language. The
brutalist direction is parked as a post-production idea until its own future phase.*

### Components And Tokens

| Feature                                 | Package or Area                   | Planned Phase | Demo-Capable Gate                                                                                                                                                                                      | Production-Ready Gate                                                                                                                                                                                        | Status                             | Notes                                                                                                                                                                                                                                                                                                                               |
|-----------------------------------------|-----------------------------------|--------------:|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| UI package foundation                   | ui                                |       Phase 9 | `@vanrot/ui` exists, builds, and exports button/tokens metadata without adding runtime weight                                                                                                          | Public exports, package docs, tree-shaking, versioning, release checks, and ownership rules verified                                                                                                         | In Progress                        | Phase 16A adds October package inventory docs, guidelines, metadata, and file-backed asset URLs; Phase 16B expands the source-owned core primitive package surface; Phase 16E adds rich forms/data metadata; Phase 16F adds interaction primitive metadata and anatomy; Phase 16H keeps optional behavior outside the core runtime. |
| Design tokens                           | ui                                |       Phase 9 | Generated app includes `src/styles/vanrot-tokens.css` and imports it once                                                                                                                              | Theme overrides, token documentation, dark/light policy, component integration, and visual QA verified                                                                                                       | In Progress                        | Phase 16A establishes October dark/light tokens, Geist text hooks, JetBrains Mono numeric hooks, radius, shadow, z-index, and motion tokens.                                                                                                                                                                                        |
| Compiler-lowered `<vr-button>`          | compiler and ui                   |       Phase 9 | `<vr-button>` lowers to a native `<button>` with base class, user class passthrough, child content, events, and property bindings                                                                      | Accessibility audit, form semantics, variant API, docs, diagnostics, and compatibility with future child components verified                                                                                 | Demo-Capable through Phase 16B     | Phase 16B keeps `<vr-button>` metadata-driven and adds static variant lowering without preserving the framework-only `variant` attribute.                                                                                                                                                                                           |
| CLI `vr add <primitive>`                | cli and ui                        |       Phase 9 | `vr add button` copies the default `ui.button.ts/html/css` primitive files and ensures token CSS exists                                                                                                | Overwrite policy, custom destinations, component registry, update workflow, docs, and multi-component support verified                                                                                       | Demo-Capable through Phase 16F     | Phase 16F supports every current primitive through the shared `@vanrot/ui` registry, including multi-word kebab file names and interaction primitives.                                                                                                                                                                              |
| CLI `vr add <local-prefix> <primitive>` | cli and ui                        |       Phase 9 | `vr add primary button` copies `primary.button.ts/html/css` into `src/ui/button/`                                                                                                                      | Naming customization docs, collision handling, aliases, feature folder support, and team conventions verified                                                                                                | Demo-Capable through Phase 16F     | Local prefix is user-owned; the role segment carries the primitive type for every Phase 16B, 16D, 16E, and 16F primitive.                                                                                                                                                                                                           |
| Typed UI primitive file roles           | ui and cli                        |       Phase 9 | Button primitives use `<local-prefix>.button.ts/html/css`                                                                                                                                              | Full typed primitive role catalog, generator integration, diagnostics, docs, and AI rules verified                                                                                                           | Demo-Capable through Phase 16F     | Phase 16F extends primitive role triplets to interaction primitives such as `.dialog`, `.drawer`, `.dropdown`, `.tabs`, and `.toast`.                                                                                                                                                                                               |
| UI primitive catalog *                  | ui                                |      Phase 16 | Additional primitive types are planned beyond button                                                                                                                                                   | Button, badge, card, avatar, separator, tabs, tooltip, dropdown, popover, toast, empty-state, skeleton, progress, accessibility, docs, examples, and visual QA verified                                      | Production-Ready through Phase 16G | Phase 16G adds popover, tooltip, and command menu primitives with registry metadata, file-backed assets, docs paths, compiler support, CLI help, and docs-shell dogfooding.                                                                                                                                                         |
| UI component parameters and variants *  | ui and compiler                   |      Phase 16 | UI components can declare basic variants without bloating runtime                                                                                                                                      | Parameter syntax, type inference, default variants, accessibility states, class merging, docs, tests, and compiler diagnostics verified                                                                      | Production-Ready through Phase 16G | Phase 16G extends strict compile-time dotted token attributes to popover side/align/size/motion, tooltip side/align/delay/tone, and command menu size/density/tone groups.                                                                                                                                                          |
| October interaction foundation          | ui, behavior, compiler, cli, docs |     Phase 16F | Dialog, drawer, dropdown, tabs, toast, popover, tooltip, and command menu ship with registry metadata, source templates, compiler lowering, CLI add/help support, behavior controllers, and docs pages | Focus behavior, keyboard behavior, accessible toast queues, tooltip disclosure, command active-descendant movement, docs routes, anatomy lowering, behavior package migration, and package verification pass | Production-Ready through Phase 16H | Phase 16H moves optional controllers into `@vanrot/behavior` so UI primitives can stay app-owned without bloating the core runtime.                                                                                                                                                                                                 |
| Tailwind interoperability               | ui and docs                       |      Phase 16 | Vanrot UI primitives allow Tailwind classes to pass through cleanly                                                                                                                                    | Setup guidance, class conflict policy, token interop, examples, docs, and build compatibility verified                                                                                                       | In Progress                        | Phase 16A adds `ui.styles: 'tailwind'` so generated apps can keep tokens while avoiding the first-party utility import.                                                                                                                                                                                                             |
| Vanrot utility class layer *            | ui                                |      Phase 16 | A first-party utility class set can be used alongside UI primitives                                                                                                                                    | PrimeFlex-inspired naming, responsive rules, spacing/display/flex/grid coverage, docs, tree-shaking or CSS budget policy, and Tailwind coexistence verified                                                  | In Progress                        | Phase 16A ships `vanrotstyles.css` with unprefixed utilities and config modes for `vanrotstyles`, `tailwind`, or `none`.                                                                                                                                                                                                            |
| UI registry and upgrade workflow        | ui and cli                        |      Phase 16 | Added UI primitives can be tracked for future update checks                                                                                                                                            | App-owned registry, diff/update policy, lock/version metadata, conflict resolution, and docs verified                                                                                                        | Deferred                           | Kept out of Phase 9 so `vr add button` stays simple.                                                                                                                                                                                                                                                                                |
| UI custom add destinations              | ui and cli                        |      Phase 16 | `vr add` can target configured folders beyond `src/ui/<type>/`                                                                                                                                         | Config schema, path validation, workspace conventions, docs, and diagnostics verified                                                                                                                        | Deferred                           | Phase 9 writes button primitives to `src/ui/button/` only.                                                                                                                                                                                                                                                                          |
| UI flavor October *                     | ui                                |     Phase 16A | October foundation assets, tokens, utilities, docs, and config mode are available                                                                                                                      | Full component coverage, accessibility, examples, visual QA, and optional behavior package boundaries verified across Phase 16B-16H                                                                          | Production-Ready through Phase 16H | Phase 16H completes the optional behavior boundary after final October interaction polish and docs-shell dogfooding.                                                                                                                                                                                                                |
| UI brutalist flavor *                   | ui                                |      Phase 17 | First-party UI components can be added using a future brutalist variation                                                                                                                              | Token separation, component parity with October, accessibility, docs, examples, and visual QA verified                                                                                                       | Deferred                           | Parked as post-production implementation until the October contracts are mature.                                                                                                                                                                                                                                                    |
| UI flavor selection                     | ui and cli                        |      Phase 17 | `vr add` or project config can choose a UI flavor without mixing styles accidentally                                                                                                                   | Migration rules, token compatibility, docs, examples, and generated output checks verified                                                                                                                   | Deferred                           | Prevents design drift once both UI flavors exist.                                                                                                                                                                                                                                                                                   |

## `@vanrot/testing`

*Testing should follow Vanrot's readable API principle: code should be understandable even to non-dev readers. Phase 18
ships the production page, router, accessibility, async, fake-timer, and generator-wide testing surface. Form and
async-resource-specific helpers remain tied to Phase 21 public APIs.*

### Test Utilities

| Feature                               | Package or Area          | Planned Phase | Demo-Capable Gate                                                                                          | Production-Ready Gate                                                                                     | Status           | Notes                                                                                                             |
|---------------------------------------|--------------------------|--------------:|------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------|------------------|-------------------------------------------------------------------------------------------------------------------|
| Testing package foundation            | testing                  |      Phase 10 | `@vanrot/testing` exists, builds, and exports the first component testing helper                           | Package exports, peer dependency policy, cleanup guarantees, docs, examples, and app integration verified | Demo-Capable     | Phase 10 owns the package shell and tiny first API.                                                               |
| `.test.ts` convention                 | testing and cli          |      Phase 10 | Generated app test files use `.test.ts`                                                                    | Generator coverage, docs, migration guidance, and framework-wide consistency verified                     | Demo-Capable     | Vanrot generated tests should not use `.spec.ts`.                                                                 |
| `testComponent(...).can(...)`         | testing                  |      Phase 10 | A compiled component can be mounted and tested through readable English-like syntax                        | Async handling, cleanup on failure, richer diagnostics, docs, and edge cases verified                     | Demo-Capable     | Wraps Vitest internally while keeping generated tests free from direct `it(...)` or `test(...)` syntax.           |
| Screen text assertion                 | testing                  |      Phase 10 | `screen.expect.text(value)` can assert text in the mounted output                                          | Visibility rules, multiple-match behavior, diagnostics, and accessibility-aware queries verified          | Demo-Capable     | Phase 10 keeps the assertion small and readable.                                                                  |
| Screen button click helper            | testing                  |      Phase 10 | `screen.click.button(label)` can click a button by readable label                                          | Accessible-name matching, disabled behavior, async updates, diagnostics, and docs verified                | Demo-Capable     | Supports a useful button interaction demo without adding a full Testing Library compatibility layer.              |
| `vr add button --test`                | cli and testing          |      Phase 10 | `vr add button --test` and `vr add <local-prefix> button --test` create a colocated `.button.test.ts` file | Collision handling, custom destinations, registry integration, docs, and update workflow verified         | Demo-Capable     | Tests remain opt-in. `vr add button` without `--test` still creates only the three source files.                  |
| Generated tests use `function` syntax | cli and testing          |      Phase 10 | Generated tests prefer `function (screen)` for readability                                                 | Docs, examples, and generator consistency verified                                                        | Demo-Capable     | TypeScript still supports arrow functions, but Vanrot-authored examples should prefer the easier-to-explain form. |
| `testPage(...)`                       | testing and router       |      Phase 18 | A route page can be tested through a readable page-focused helper                                          | Router setup, route params, navigation, lazy pages, cleanup, docs, and diagnostics verified               | Production-Ready | `runPageTest(...)` exposes direct jsdom page mounting, rerender, cleanup, screen access, and lifecycle teardown.  |
| `vr generate component --test`        | cli and testing          |      Phase 18 | Component generation can optionally create `.component.test.ts`                                            | Naming, copy source, import typing, overwrite policy, docs, and examples verified                         | Production-Ready | Generated component tests use `testComponent(...)` and keep test files opt-in.                                    |
| `vr generate page --test`             | cli, router, and testing |      Phase 18 | Page generation can optionally create `.page.test.ts`                                                      | `testPage(...)`, router setup, route copy, lazy page support, docs, and examples verified                 | Production-Ready | Generated page tests use `testPage(...)`; route-specific assertions live in `setupRouterTest(...)`.               |
| Advanced testing helpers              | testing                  |      Phase 18 | Tests can cover richer user workflows without losing readability                                           | Router navigation, accessibility assertions, async coordination, fake timers, cleanup, and docs verified  | Production-Ready | Forms and async-resource-specific helpers stay deferred until Phase 21 stabilizes those APIs.                     |

## Docs *

*Documentation needs a dedicated completeness pass before production. The goal is high-depth documentation where every
package, command, public API, generated file, convention, limitation, and production caveat is covered so neither users
nor AI assistants have to infer missing framework behavior.*

### Public Documentation

| Feature                           | Package or Area | Planned Phase | Demo-Capable Gate                                                                 | Production-Ready Gate                                                                                       | Status                            | Notes                                                                                                                                                                                    |
|-----------------------------------|-----------------|--------------:|-----------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------|-----------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| API docs *                        | docs            |      Phase 24 | Public APIs are documented                                                        | Examples, versioning, generated docs, completeness checks, and release workflow verified                    | Production-Ready                  | Phase 24 adds the framework reference registry and public API docs coverage checks.                                                                                                      |
| Guide docs *                      | docs            |      Phase 24 | Guides explain core user workflows such as create, build, route, add UI, and test | Examples, screenshots where useful, code freshness checks, and release workflow verified                    | Production-Ready                  | Phase 24 expands the framework docs path with devtools, public API, diagnostics, generated files, limitations, deployment, and example matrix guides.                                    |
| Deep documentation system *       | docs            |      Phase 24 | Docs cover every package, feature, CLI command, convention, and known limitation  | Package inventory, examples, migration notes, API references, guides, and completeness checks verified      | Production-Ready                  | Phase 24 creates a structured docs registry for packages, exports, commands, diagnostics, generated files, conventions, examples, limitations, maturity, deployment, and route metadata. |
| Documentation completeness checks | docs and ci     |      Phase 24 | CI can detect undocumented public packages, commands, and feature-maturity rows   | Automated docs inventory, broken link checks, example freshness, and release checklist integration verified | Production-Ready                  | `verify:site-docs` now checks package, export, command, diagnostic, generated file, convention, example, CTA, route metadata, and docs-shell coverage.                                   |
| AI-readable documentation bundle  | docs and ai     |      Phase 25 | Documentation can be exported into a stable AI-consumable bundle                  | Versioned manifest, package summaries, examples, limits, changelog links, and validation checks verified    | Production-Ready through Phase 25 | Feeds MCP, Skill.sh, and first-party `vr ai` commands with authoritative framework knowledge.                                                                                            |

## Web Presence *

*Vanrot will live at `vanrot.vankode.com`. The docs, install guide, landing page, and UI documentation should feel like
one coherent product, not scattered artifacts.*

### `vankode.com` Subdomain

| Feature                      | Package or Area         | Planned Phase | Demo-Capable Gate                                                                                                                                  | Production-Ready Gate                                                                                                  | Status                            | Notes                                                                                                                                               |
|------------------------------|-------------------------|--------------:|----------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------|-----------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------|
| Vanrot subdomain site *      | web and docs            |     Phase 16C | `apps/vanrot-site` has landing, install, implemented framework docs, component pages, reference data, and docs drift checks                        | Domain routing, deployment, navigation, SEO, accessibility, analytics policy, and release docs workflow verified       | Production-Ready through Phase 24 | Phase 24 makes the site deployment-ready for `vanrot.vankode.com` without performing DNS, credentials, analytics, or live launch work.              |
| Landing page                 | web                     |     Phase 16C | Landing page explains Vanrot clearly and directs users to install/docs                                                                             | Real copy, responsive design, performance, accessibility, screenshots, and deployment verified                         | Production-Ready through Phase 24 | Phase 24 renames the public CTAs to `Framework Documentation` and `Design Component` and polishes the landing surface.                              |
| Install guide                | web and docs            |     Phase 16C | Users can install Vanrot through the recommended package manager path                                                                              | npm, pnpm, bun, yarn, and Homebrew paths are tested and documented where supported                                     | Production-Ready through Phase 24 | Phase 24 keeps current install guidance honest while distribution-complete package-manager verification remains Phase 26.                           |
| Framework documentation site | web, docs, and packages |     Phase 16C | Users can browse current framework guides, commands, package references, diagnostics, maturity status, and `@vanrot/ui` primitive docs and gallery | Component examples, tokens, accessibility notes, copyable code, package references, visual QA, and versioning verified | Demo-Capable through Phase 16F    | Phase 16F keeps the docs shell example-only for interaction primitives and publishes dedicated pages for dialog, drawer, dropdown, tabs, and toast. |

## AI Consumption *

*When Vanrot is production-ready, AI tools should be able to consume the framework directly. Codex and Claude should
have a reliable way to understand Vanrot even when users do not know the docs well.*

### MCP And Skill.sh

| Feature                         | Package or Area | Planned Phase | Demo-Capable Gate                                                                                | Production-Ready Gate                                                                                                    | Status                            | Notes                                                                           |
|---------------------------------|-----------------|--------------:|--------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------|-----------------------------------|---------------------------------------------------------------------------------|
| Vanrot MCP server *             | ai and docs     |      Phase 25 | An MCP server can answer framework questions from authoritative Vanrot docs and package metadata | Local stdio MCP server exposes versioned bundle resources, typed search, and verified docs/commands/diagnostics/examples | Production-Ready through Phase 25 | Remote distribution and external client packaging remain Phase 26 release work. |
| Skill.sh package *              | ai and docs     |      Phase 25 | Vanrot framework instructions can be installed or consumed by AI coding tools                    | Generated Skill.sh files point to the official manifest and rules and are checked by `verify:ai-docs`                    | Production-Ready through Phase 25 | Public distribution remains Phase 26; local bundle output is verified.          |
| AI framework knowledge manifest | ai and docs     |      Phase 25 | Docs expose a structured manifest of packages, commands, rules, examples, and limitations        | Schema versioning, source fingerprints, generated docs alignment, and drift checks pass through `pnpm verify`            | Production-Ready                  | Shared source for MCP, Skill.sh, Codex, Claude, and other future AI consumers.  |

## Distribution *

*Distribution should stay familiar for JavaScript users, but production Vanrot also needs native-feeling CLI
installation paths.*

### Release And Registry

| Feature                           | Package or Area           | Planned Phase | Demo-Capable Gate                                              | Production-Ready Gate                                                                                                              | Status                            | Notes                                                                                                                                       |
|-----------------------------------|---------------------------|--------------:|----------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------|
| npm package publishing            | distribution and packages |      Phase 26 | Packages can be packed locally and installed into fixture apps | Public package names, provenance, access policy, release automation, smoke installs, and rollback policy verified                  | Dry-Run Ready through Phase 26    | `verify:release-dry-run` packs every public package and proves local pnpm/npm consumers can install the tarballs before any real publish.   |
| Package provenance and release CI | distribution and ci       |      Phase 26 | Release artifacts are generated by repeatable scripts          | Provenance, checksums, artifact inspection, permission boundaries, signed tags if chosen, and CI-only publish credentials verified | Dry-Run Ready through Phase 26    | The local gate captures repeatable tarballs, report artifacts, and failure evidence; real CI credentials remain outside this dry-run phase. |
| Versioning and changelog workflow | distribution and repo     |      Phase 26 | Packages can share a coherent versioning strategy              | Changesets or equivalent workflow, changelog quality, migration notes, deprecation policy, and release notes verified              | Dry-Run Ready through Phase 26    | Public packages now expose release metadata and `engines.node`; changelog automation remains a publish-time policy choice.                  |
| Install verification matrix       | distribution, cli, docs   |      Phase 26 | One package manager path can install and run a sample app      | npm, pnpm, bun, yarn, Homebrew where supported, clean machine checks, and docs examples verified                                   | Production-Ready through Phase 26 | `verify:release-dry-run` passes pnpm consumer install/smoke/build, npm install/smoke, and Homebrew local tap install when `brew` exists.    |

### Homebrew

| Feature                                  | Package or Area      | Planned Phase | Demo-Capable Gate                                                 | Production-Ready Gate                                                                            | Status                         | Notes                                                                                                                                                           |
|------------------------------------------|----------------------|--------------:|-------------------------------------------------------------------|--------------------------------------------------------------------------------------------------|--------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Homebrew install `brew install vanrot` * | distribution and cli |      Phase 26 | Vanrot CLI can be installed through Homebrew on supported systems | Formula, release artifact policy, checksums, upgrades, uninstall behavior, CI, and docs verified | Dry-Run Ready through Phase 26 | The verifier builds a local tap formula from packed tarballs, installs `vr`, runs the formula test, and cleans the tap; public tap publishing remains separate. |

## Platform Packages

### SSR And Hydration

| Feature       | Package or Area | Planned Phase | Demo-Capable Gate                            | Production-Ready Gate                                       | Status           | Notes                                                                                                                      |
|---------------|-----------------|--------------:|----------------------------------------------|-------------------------------------------------------------|------------------|----------------------------------------------------------------------------------------------------------------------------|
| SSR-safe APIs | ssr             |      Phase 22 | Browser APIs are isolated behind `onMount()` | Server render, hydration, routing, and diagnostics verified | Production-Ready | Shipped through opt-in `@vanrot/ssr`; runtime remains browser-first.                                                       |
| Hydration     | ssr             |      Phase 22 | Static markup can attach client behavior     | Mismatch diagnostics and streaming strategy verified        | Production-Ready | `hydrate(...)` attaches explicit hydratable modules and reports deterministic mismatch diagnostics; streaming is deferred. |

### Async, Forms, And Devtools

| Feature         | Package or Area | Planned Phase | Demo-Capable Gate                             | Production-Ready Gate                                                  | Status                            | Notes                                                                                                                                                                                   |
|-----------------|-----------------|--------------:|-----------------------------------------------|------------------------------------------------------------------------|-----------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Async resources | async           |      Phase 21 | Basic async state primitive works             | Cancellation, cache policy, suspense policy, and SSR behavior verified | Deferred                          | Optional package.                                                                                                                                                                       |
| Forms           | forms           |      Phase 21 | Basic form state works                        | Validation, accessibility, and two-way binding policy verified         | Deferred                          | Optional package.                                                                                                                                                                       |
| Devtools        | devtools        |      Phase 23 | Runtime graph can be inspected in development | Browser extension or panel integration verified                        | Production-Ready through Phase 23 | `@vanrot/devtools` ships the graph contract, panel state, extension asset flow, runtime graph event contract, and the current Web Types route shorthand metadata used by the docs site. |

## Examples

### Demo Apps

| Feature          | Package or Area | Planned Phase | Demo-Capable Gate                                | Production-Ready Gate                                                  | Status       | Notes                                                                                                                                                                                      |
|------------------|-----------------|--------------:|--------------------------------------------------|------------------------------------------------------------------------|--------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Counter demo app | examples        |       Phase 6 | Counter compiles and updates without virtual DOM | Demo documents runtime, compiler, Vite, and CLI integration boundaries | Demo-Capable | Phase 6 verified a workspace example app with grouped component files, signal updates, scoped CSS, Vite build output, CLI build/test/doctor workflows, and no production hardening claims. |

## Production Phase Details

Each production phase must get its own brainstorming session, design spec, implementation plan, execution pass, review,
and ledger update. Use this table to keep the build order stable.

Broad production phases must be sliced before implementation. The numbered phase keeps the public roadmap stable, while
the slice map below tells future planning which smaller specs/plans should exist inside that phase. Do not mark a broad
phase `Production-Ready` just because one slice is complete.

Future numbered phases and tooling candidates that are not active, shipped, or explicitly dropped are parked in
`docs/superpowers/future-pipeline.md` until those phases or candidates become active.

| Phase | Primary Package Or Area        | Modules                           | Submodules                                                                                                                                                         | Main Output                                                                      |
|------:|--------------------------------|-----------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------|
|    11 | repo and docs                  | Production standards              | Roadmap checklist, readiness gates, package/module ownership, status vocabulary                                                                                    | This ledger becomes the numbered production source of truth.                     |
|    12 | runtime, compiler, vite-plugin | Core hardening                    | Reactive edge cases, lifecycle cleanup, component typing, compiler control flow, diagnostics, source maps, CSS scoping, Vite HMR                                   | Demo core becomes dependable enough for production app foundations.              |
|    13 | config, cli, vite-plugin       | Project configuration             | `vanrot.config.ts`, schema defaults, validation, migrations, install-aware config blocks, port `1010`, config-driven conventions                                   | Project configuration becomes the single source of truth for framework settings. |
|    14 | cli                            | CLI production experience         | Terminal design language, command journeys, `vr dev`, `vr build`, `vr test`, `vr doctor --inspect`, `vr cache clean`, starter localhost page                       | The CLI becomes beautiful, readable, scriptable, and production-friendly.        |
|    15 | router                         | Router production                 | Query strings, nested routes, layouts, redirects, guards, active links, typed param links, route diagnostics, preloading, keepAlive route views, navigation polish | Real app routing works without repeated route string literals.                   |
|    16 | ui                             | UI October                        | Primitive catalog, variants, parameters, accessibility, token system, registry, Tailwind interop, `vanrotstyles.css` utility classes                               | The shadcn-inspired Vanrot UI October baseline becomes production-ready.         |
|    17 | ui                             | Post-production brutalist UI      | Brutalist flavor, flavor selection, token separation, component parity, visual QA                                                                                  | The brutalist flavor ships on the same component contracts as October.           |
|    18 | testing, cli, router           | Post-production testing           | `testPage(...)`, component/page `--test`, router testing, accessibility assertions, async helpers, fake timers, forms testing                                      | Vanrot tests cover realistic user workflows with readable APIs.                  |
|    19 | store                          | Post-production store foundation  | Store package, state containers, actions, reducers, selectors, full effects, immutable updates, signal-native reads                                                | Vanrot gets a lightweight enterprise state foundation.                           |
|    20 | store, devtools, rxjs          | Post-production store hardening   | Deeper cancellation and concurrency inspection, action tracing, time travel, Redux interop, RxJS bridge                                                            | Enterprise teams get advanced state workflows without making RxJS the default.   |
|    21 | forms and async                | Post-production forms and async   | Form state, validation, field metadata, async resources, cancellation, loading/error conventions, cache policy                                                     | Vanrot supports form-heavy and async-heavy apps with first-party patterns.       |
|    22 | ssr                            | Post-production SSR and hydration | SSR-safe APIs, server rendering, hydration, mismatch diagnostics, router integration, streaming strategy                                                           | Vanrot can render and hydrate beyond client-only demos.                          |
|    23 | devtools and intelligence      | Devtools and project intelligence | Runtime graph inspection, route graph, component graph, compiler-aware project map, AI rules customization                                                         | Humans and AI tools can inspect real Vanrot apps through authoritative metadata. |
|    24 | docs and web                   | Documentation and public site     | Deep docs, API docs, guide docs, completeness checks, `vanrot.vankode.com`, install guide, UI docs                                                                 | Users get full documentation after the production feature surface is stable.     |
|    25 | ai and docs                    | AI consumption                    | AI-readable docs bundle, knowledge manifest, MCP server, Skill.sh package, optional AI commands                                                                    | Codex, Claude, and other AI tools can consume official Vanrot knowledge.         |
|    26 | distribution and release       | Release hardening                 | npm publishing, provenance, CI release checks, versioning, changelog, install verification, Homebrew formula                                                       | Vanrot can be publicly installed, upgraded, audited, and released safely.        |

## Production Slicing Map

Use this as the first check during every future `/brainstorm`, `/writing-plan`, and `/execution-plan` pass. If a phase
still feels too wide after applying its listed slices, split it again and keep the unfinished requirements tracked here.
For future numbered phases and tooling candidates, also check `docs/superpowers/future-pipeline.md` before opening a
new brainstorm.

| Phase | Slice Needed | Recommended Slices                                                                                                                                                                                                                                                                                         | Notes                                                                                                                                                          |
|------:|--------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------|
|    11 | No           | Standards foundation was completed as one repo/docs slice.                                                                                                                                                                                                                                                 | Keep future changes scoped to the maturity ledger, verifier, and hook rules.                                                                                   |
|    12 | No           | 12A core audit and failing tests; 12B runtime production hardening; 12C compiler diagnostics and source locations; 12D Vite dev/build/HMR hardening; 12E TypeScript contracts and maturity gates.                                                                                                          | Completed across 12A through 12E; keep later config, router, UI, testing, and store hardening in their own phases.                                             |
|    13 | Yes          | 13A config shape and defaults; 13B schema validation and diagnostics; 13C Vite/CLI config loading; 13D install-aware config updates; 13E generated app migration and port `1010` verification.                                                                                                             | Keep root project structure familiar while making config the single source of truth.                                                                           |
|    14 | Yes          | 14A CLI command contract; 14B beautiful reporter system; 14C `vr dev/build/test` wrappers; 14D `vr doctor --inspect` and `vr cache clean`; 14E starter localhost experience.                                                                                                                               | Terminal UX needs design review before implementation, especially the Claude Code-inspired feel.                                                               |
|    15 | Yes          | 15A route contract production; 15B nested layout routing; 15C redirects, guards, and route graph diagnostics; 15D preloading and route integration tests; 15E navigation polish.                                                                                                                           | Completed across 15A through 15E; keep future router graph tooling, route data loaders, and SSR routing in later phases.                                       |
|    16 | Yes          | 16A October foundation, metadata, tokens, `vanrotstyles.css`, and config style modes; 16B core primitives; 16C Vanrot learning site base; 16D layout/navigation/media primitives; 16E forms/data primitives; 16F interaction foundation; 16G final October polish; 16H optional behavior package boundary. | 16A through 16H complete the October UI system plus the runtime/behavior split; keep Phase 17-22 post-production candidates separate unless explicitly opened. |
|    17 | Yes          | 17A flavor architecture; 17B brutalist tokens; 17C component parity checks; 17D flavor selection tooling; 17E visual QA and docs hooks.                                                                                                                                                                    | Future flavors must share contracts with October so users can switch style without changing component mental models.                                           |
|    18 | Yes          | 18A `testPage(...)`; 18B router/page testing helpers; 18C accessibility assertions; 18D async/fake timer helpers; 18E generator-wide `--test` support.                                                                                                                                                     | Keep test syntax readable for non-devs and avoid clever shorthand.                                                                                             |
|    19 | Yes          | 19A store brainstorming and API vocabulary; 19B signal-native store core; 19C actions, reducers, selectors, and immutable updates; 19D package boundaries; 19E starter examples.                                                                                                                           | Store needs a dedicated design cycle because it must be lighter and more future-facing than NgRx.                                                              |
|    20 | Yes          | 20A effects and cancellation; 20B concurrency and action tracing; 20C time travel and devtools bridge; 20D Redux compatibility; 20E RxJS interop.                                                                                                                                                          | RxJS should be supported without becoming the default mental model.                                                                                            |
|    21 | Yes          | 21A form state and field metadata; 21B validation and messages; 21C async resource primitive; 21D cancellation and cache policy; 21E form/resource testing helpers.                                                                                                                                        | Forms and async resources should stay first-party patterns without bloating the runtime kernel.                                                                |
|    22 | Yes          | 22A SSR-safe API audit; 22B server rendering; 22C hydration and mismatch diagnostics; 22D router integration; 22E streaming strategy.                                                                                                                                                                      | Preserve the browser-only runtime boundary until SSR APIs are explicitly designed.                                                                             |
|    23 | Yes          | 23A project map graph; 23B route and component graphs; 23C runtime graph inspection; 23D devtools metadata transport; 23E AI rules customization.                                                                                                                                                          | Human and AI inspection should use authoritative local metadata, not guesses.                                                                                  |
|    24 | Yes          | 24A package/API inventory; 24B deep docs audit; 24C guide docs and examples audit; 24D `vanrot.vankode.com` production hardening; 24E docs completeness checks.                                                                                                                                            | Phase 16C creates the site early; Phase 24 remains the exhaustive final audit once the production feature surface stabilizes.                                  |
|    25 | Yes          | 25A AI-readable docs bundle; 25B knowledge manifest; 25C MCP server; 25D Skill.sh package; 25E AI command/context integration.                                                                                                                                                                             | Codex and Claude should consume official Vanrot knowledge when users do not know the framework yet.                                                            |
|    26 | Yes          | 26A npm publishing and provenance; 26B release CI and changelog/versioning; 26C install verification matrix; 26D Homebrew formula; 26E public release security checklist.                                                                                                                                  | Keep npm as canonical unless a future release-security decision changes the distribution model.                                                                |

## Update Rule

Every phase plan must include a final ledger update step.

A feature must not move to `Demo-Capable` unless the phase acceptance gate has been verified.

A feature must not move to `Production-Ready` unless it has:

- documented public behavior
- focused tests for normal and edge cases
- integration tests for the expected user workflow
- diagnostics for common misuse
- no known deferred behavior hidden behind the same status 
