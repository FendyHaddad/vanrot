{
  "name": "@vanrot/example-formatters-pipes",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "pretest": "pnpm --filter @vanrot/formatters build && pnpm --filter @vanrot/compiler build",
    "test": "vitest run",
    "pretypecheck": "pnpm --filter @vanrot/formatters build && pnpm --filter @vanrot/compiler build && pnpm --filter @vanrot/config build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@vanrot/compiler": "file:../../packages/compiler",
    "@vanrot/config": "file:../../packages/config",
    "@vanrot/formatters": "file:../../packages/formatters"
  },
  "devDependencies": {
    "vitest": "^4.0.14"
  }
}
