{
  "name": "@vanrot/example-build-deploy",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vr build",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@vanrot/config": "file:../../packages/config",
    "@vanrot/vite-plugin": "file:../../packages/vite-plugin"
  },
  "devDependencies": {
    "@vanrot/cli": "file:../../packages/cli",
    "typescript": "^6.0.3",
    "vite": "^8.0.10",
    "vitest": "^4.0.14"
  }
}
